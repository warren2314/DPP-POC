{
  "name": "dpp-assessment-platform",
  "private": true,
  "packageManager": "pnpm@10.6.0",
  "scripts": {
    "dev:web": "pnpm --filter @dpp/web dev",
    "dev:api": "pnpm --filter @dpp/api start:dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "seed": "pnpm --filter @dpp/api prisma db seed"
  }
}
